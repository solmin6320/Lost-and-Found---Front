# 인수인계

다른 로컬 환경에서 이 프로젝트를 이어서 작업하기 위한 문서.

**갱신 규칙 — 작업 단위가 끝날 때마다 "현재 상태"와 "다음 할 일"을 고친다.** 토큰이 떨어진 뒤에 쓰면 이미 늦다.

최종 갱신: **2026-09-26**

---

## 1. 프로젝트 개요

**분실물 찾기 서비스.** 김솔민(solmin6320)의 개인 학습 프로젝트. 목표 완료일 **2026-10-20**.

리포지토리가 둘이다. **서로 건드리지 않는다.**

| | 백엔드 | 프론트 |
|---|---|---|
| 리포 | [Lost-and-Found](https://github.com/solmin6320/Lost-and-Found) | [Lost-and-Found---Front](https://github.com/solmin6320/Lost-and-Found---Front) |
| 로컬 | `C:\Users\solmi\Downloads\Lost-and-Found` | `C:\Users\solmi\Downloads\Front` |
| 스택 | Spring Boot 4.1 · Java 21 · MariaDB · Redis | React · TypeScript · Vite |
| 목적 | **학습** — 개념을 이해하며 진행 | **화면 확보**(포트폴리오) — 완성물 전달 |

### 명세서 (백엔드, git 밖)

```
C:\Users\solmi\Downloads\분실물찾기_기능명세서.md
C:\Users\solmi\Downloads\분실물찾기_DB명세서.md
```

**`.md` 가 기준이다.** 같은 폴더의 `(1).txt` 는 폐기된 옛 판본이다(2026-09-23).

⚠️ 바탕화면에 **낡은 사본**이 있다. 절대 읽거나 고치지 않는다.

---

## 2. 작업 규칙 (중요)

| | 백엔드 | 프론트 |
|---|---|---|
| 파일 생성·수정 | **금지.** 코드를 텍스트로 전달하면 본인이 붙여넣는다 | **허용** |
| `git commit` / `push` | **본인만** | 허용 (프론트 리포에만) |
| 진행 방식 | 왜 → 역할 → 흐름 → 전체 코드 → 복습 | 완성물 전달 |
| 커밋 단위 | 짧게. 각 커밋이 단독으로 빌드 통과 | 완결된 기능 묶음. 에이전트 1회당 2~3개, 각 커밋이 단독으로 빌드 통과 |

- **병행 개발.** "백엔드 끝내고 프론트"가 아니라 둘 다 계속 업데이트한다
- 10/20은 **백엔드 배포** 목표다. 둘 중 하나를 미뤄야 하면 프론트를 미룬다
- 주석은 짧게. 무엇을 하는지만
- 기존 코드를 리팩터링하지 않는다. 추가(add-only)로 간다. 버그는 예외적으로 보고

---

## 3. 현재 상태 (2026-09-26)

### 백엔드 — 기능 13개 엔드포인트 동작

```
POST   /api/auth/signup · login · reissue · logout
GET    /api/members/me      PATCH /api/members/me      PATCH /api/members/me/password
POST   /api/posts           GET /api/posts             GET /api/posts/{id}
PUT    /api/posts/{id}      DELETE /api/posts/{id}     PATCH /api/posts/{id}/status
```

- 단위 테스트 **50개 통과** (`./gradlew test`)
- CI: `ci.yml`(빌드+단위테스트) · `codeql.yml`(보안)
- PR #21 머지 완료 — 게시글 수정·삭제 단위 테스트. `main` 125커밋

**미구현**

| | 필요한 것 |
|---|---|
| `[5.1]` 댓글 쓰기 | `CommentController` 전체 + `COMMENT_NOT_FOUND` ErrorCode + **`V2__add_comment_updated_at.sql` 선행** |
| `[6.1]` 마이페이지 | `GET /api/members/me/posts` |

댓글 **읽기는 된다** — `PostDetailResponse.comments` 에 처음 20건.

### 프론트 — 단계 2 로그인 · 회원가입 · 온보딩 완료, 목록 썸네일 연결

- **API·인증 기반** : HTTP 클라이언트(`credentials: 'include'`), 액세스 토큰 메모리 저장, 401 재발급 단일 비행 + 탭 간 직렬화(Web Locks), 인증 Context, 목록 쿼리 훅
- **팔레트** : 중성 잉크 + 흰 종이. **색은 개념에 붙는다** — 분실 마리골드 · 습득 코발트를 면 단계(연한 바탕 · 선 · 진한 면 · 글자)까지 `tokens.css`에 정의. "색은 유형 배지에만" 규칙은 폐기
  - 2026-09-26 코발트 `#2F5CE0` → `#2F5FB1`(Tailwind 파랑과 같아 보였다), 마리골드 넓은 면만 `#F5BE3D`. 전후 비교 스크린샷 있음 — **본인이 원래 색을 고르면 되돌린다**
- **다크 모드** : 의미 토큰 위에 어둡게 팔레트. 설정 화면에서 고르고(`localStorage theme:v1`), 안 고르면 기기 설정. 첫 화면 전 적용은 `public/theme-init.js`(CSP 때문에 인라인 아님)
- **시각 체계** : 아이콘 Phosphor(`bold`) · 반경 4단계(4 · 8 · 12 · 알약) · 눌림 한 가지(투명도). 컴포넌트는 의미 토큰만 쓴다
- **공통 UI** : 버튼 · 스켈레톤 · 빈 상태 · 오류 상태 · 시트(좁은 화면 바텀 시트 · 넓은 화면 다이얼로그) · 페이지 이동 · 유형/상태 배지(`features/posts/ui`, 사진 위 변형 포함)
- **헤더** : 로고(꼬리표 두 장, 23rem 미만은 글자 접음) · [+ 글 올리기](좁으면 [+]) · 서비스 안내(물음표) · 설정(톱니) · 로그인/계정 메뉴. 320px 한 줄
- **SCR-01 목록(2026-09-25 재설계, 9/26 검수 반영)** : 맨 위 의도 선택(`잃어버렸어요` → 습득 글 · `주웠어요` → 분실 글, 조건이 없을 때만 건수) · 큰 검색창 · 필터 칩(맨 앞 [전체 해제]) + 시트 · 정사각 사진 피드 2/3/4열(쪽당 24건) · 페이지 · 의도별 "분실/습득 글 올리기" · 네 가지 상태. 전부 URL 쿼리스트링 동기화. 사라지는 버튼을 누르면 포커스는 결과 제목으로
- **SCR-08 설정 `/settings`(9/26 앞당김)** : 누구나. 프로필(이메일 · 가입일 · 닉네임, 로그인) → 화면 모드 두 칸(실제 토큰 미리보기) → 비밀번호 변경(로그인). 비로그인은 로그인 권유 한 장
- 1차 목록(왼쪽 필터 열 + 회색 가로형 목록)은 본인이 거부했다 — 딱딱하고 칙칙함 · 분실/습득 구분이 약함 · 초라함
- **SCR-05 로그인 · SCR-06 회원가입(9/26)** : `?redirect=` 앱 안 경로만, 돌아갈 곳 미리 알림, 401 폼 위 · 423 이메일 단위 잠금 · 409 필드 아래, 가입하면 바로 로그인 → 돌아갈 곳(목록이면 온보딩 신호). 넓은 화면 두 칸(글 두 갈래 안내). 추후 OAuth 자리만 있음(버튼 없음)
- **온보딩(9/26)** : 1층 — 가입 직후 한 번 3단계(의도 선택 두 칸 → 검색 · 필터 → [글 올리기]), 잉크 테로만 강조, 좁은 화면 바텀 시트 · 넓은 화면 대상 옆 카드, `onboarding:v1:done`. 헤더 `서비스 안내`로 누구나 다시 연다. 2층 — 목록 `이름표 안내`(누르면 펼침) · 필터 시트 상태 도움말. 코드는 `src/app/onboarding/`, 설계 · 결과는 `docs/온보딩설계.md`
- **목록 썸네일 연결(9/26)** : `PostListResponse.thumbnailUrl`(없으면 `null`). 사진 없는 글은 계속 **포스터**
- **모션** : "부드럽게 이어 주는 화면" — 폼 오류 · 안내 등장, 사진 떠오름, 화면 모드 교차 전환. 기준은 디자인품질기준 6장
- 나머지 5개 화면(상세 · 등록 · 수정 · 내가 쓴 글 · 404)은 자리표시자. `npm run dev` · `build` · `typecheck` · `lint` 통과
- 목 서버(가짜 사진 SVG 포함)·스크린샷은 리포 밖(세션 스크래치패드)에 있었다. 새 환경에서는 없다

---

## 4. 새 로컬 환경 세팅

### 공통

- **Node.js LTS** (v24.x) — `winget install OpenJS.NodeJS.LTS`
- **JDK 21** (temurin)
- **Docker Desktop** — 백엔드의 MariaDB·Redis용

### 백엔드

```bash
git clone https://github.com/solmin6320/Lost-and-Found.git
cd Lost-and-Found
docker compose up -d          # MariaDB 3306, Redis 6379
./gradlew test                # 단위 테스트 (DB 불필요)
./gradlew integrationTest     # 통합 테스트 (DB 필요)
```

⚠️ **`application-local.yml` 은 git에 없다.** AWS 키·DB 비밀번호가 들어 있어 gitignore 대상이다. 본인이 직접 만들어야 한다. 필요한 키는 `application.yml` 의 `${...}` 자리를 보면 된다.

### 프론트

```bash
git clone https://github.com/solmin6320/Lost-and-Found---Front.git Front
cd Front
npm ci
cp .env.example .env
npm run dev                   # http://localhost:5173
```

API 호출을 하려면 백엔드(8080)와 Docker 컨테이너가 떠 있어야 한다. Vite 프록시가 `/api` 를 8080으로 넘긴다.

---

## 5. Claude 구성

### 서브에이전트 (`~/.claude/agents/`)

| | 역할 |
|---|---|
| `front-architect` | 환경설정 · 폴더구조 · 라우팅 · 화면 목록 |
| `front-api` | 백엔드 DTO → TS 타입, API 클라이언트, 401 재발급 인터셉터 |
| `front-ui` | 페이지 · 컴포넌트 구현 |
| `front-review` | 계약 일치 · 보안 · 반응형 검수 (읽기 전용) |

### 스킬 (`~/.claude/skills/`)

| | 용도 |
|---|---|
| `spec-check` | 백엔드 기능 착수 전 명세서 대조 |
| `pr-body` | PR 본문 초안 |
| `front-design-rules` | 프론트 디자인·UX 구체 규칙 |
| `front-security-rules` | 프론트 보안 규칙 |

**에이전트·스킬 정의는 git에 없다.** 새 환경에서는 다시 만들어야 한다. 내용의 본체는 이 리포의 `docs/` 에 있으므로, 그것을 읽고 얇은 스킬로 감싸면 된다.

### 이 리포의 문서

```
docs/화면정의서.md        화면 9개 상세 정의
docs/디자인품질기준.md     품질 기준 — "끝났다"의 정의
docs/보안명세서.md        XSS · 토큰 · CSP · 공급망
README.md                실행법 · 폴더 규칙
```

---

## 6. 다음 할 일

### 백엔드

1. **`[5.1]` 댓글** (진행 중) — `V2__add_comment_updated_at.sql` 먼저. 2일
2. **`[6.1]` 마이페이지** — `GET /api/members/me/posts`. 1일
3. 헬스체크(actuator). 0.5일
4. 통합 테스트 + Postman 일괄 검증. 2일
5. **AWS 배포** 10/5~10/15. 8~10일

### 프론트

1. **본인 스크린샷 확인** — 로그인 · 회원가입, 오류 화면, 사진 섞인 목록, **온보딩 3단계 · 헤더 서비스 안내 · 이름표 안내**(320 헤더는 로고 글자를 접었다 — 확인받을 것)
2. **단계 3 상세(SCR-03)** — 상태 변경, 삭제 확인 다이얼로그, 댓글 읽기. 상태 배지 설명은 `Toggletip` + `POST_STATUS_MEANING` 재사용
3. 남은 온보딩 2층 — 등록 · 상세 · 마이페이지 자리(`docs/온보딩설계.md` 4장 표)
4. 세션 만료 → 로그인 화면으로(`redirect` + "로그인이 만료됐습니다") — 로그인 화면은 받을 준비가 돼 있다
5. 단계 4 등록 화면 — 목록이 보내는 `?type=LOST|FOUND`로 유형을 미리 고른다
6. 추후 OAuth(구글 · 네이버) — 백엔드가 생기면 `AuthLayout`의 `alternatives`에 버튼 묶음

### 밀린 작은 것들

- CodeQL `Disabled Spring CSRF protection` 경고 → **Dismiss(`Won't fix`)** + 근거 코멘트
- GitHub Branch protection — `main` 에 required status checks
- CI에 테스트 리포트 아티팩트 업로드 (`if: always()`)
- `AuthService` 의 `.secure(true)` 하드코딩 → yml로 분리 (로컬 false / 운영 true)

---

## 7. 알려진 함정

### 백엔드

- **Docker 컨테이너가 내려가면** `integrationTest` 가 `ConnectException` 으로 실패한다. `docker compose up -d`
- 윈도우 MariaDB 서비스가 자동 시작으로 되돌아와 3306을 점유하면 `SQLInvalidAuthorizationSpecException` 이 난다. `Set-Service MariaDB -StartupType Manual` (`net stop` 은 재부팅까지만)
- `@ExceptionHandler` · 애노테이션을 추가하면 **앱을 재시작**해야 한다. HotSwap은 등록하지 않는다
- Flyway V1은 이미 적용됐다. 컬럼 추가는 **V2 이상의 새 파일**로. V1을 고치면 체크섬 불일치로 기동이 막힌다
- `@ModelAttribute` 는 레코드 컴포넌트 이름을 폼 필드로 쓰고 **대소문자를 구분**한다

### 프론트

- Node 설치 직후에는 현재 세션 PATH에 안 잡힌다. 새 터미널을 열거나 `export PATH="/c/Program Files/nodejs:$PATH"`
- PowerShell에서 한글 테스트명이 깨지면 `chcp 65001`
- `VITE_` 접두사 변수는 번들에 그대로 박힌다. 비밀값 금지
- 액세스 토큰은 5분. 만료되면 `permitAll` 경로에서 401이 아니라 **200이 조용히 나간다**

---

## 8. 결정 기록이 있는 곳

- **백엔드 설계 판단** — 기능명세서 각 항목의 "결정" 절, DB명세서 각주
- **프론트 화면 동작** — `docs/화면정의서.md`
- **왜 그렇게 했는지** — 각 PR 본문의 "설계 판단" 절. GitHub에 남아 있다
